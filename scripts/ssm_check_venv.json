{
  "commands": [
    "ls -la /opt/pytorch/bin/ | grep -E 'python|pip' || echo 'no python in bin'",
    "/opt/pytorch/bin/python -c 'import torch; print(\"torch\", torch.__version__, \"cuda\", torch.cuda.is_available(), torch.cuda.get_device_name(0))'"
  ]
}
